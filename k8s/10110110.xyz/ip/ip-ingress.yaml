---
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: redirect-http
  namespace: iofq
spec:
  redirectScheme:
    scheme: https
    permanent: true

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ip-ingress-https
  namespace: iofq
  annotations:
    cert-manager.io/issuer: "iofq-cloudflare-issuer"
    kubernetes.io/ingress.class: "traefik"
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
spec:
  tls:
  - hosts:
    - ip.10110110.xyz
    secretName: ip-tls
  rules:
  - host: ip.10110110.xyz
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ip-service
            port:
              number: 8080

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ip-ingress-https
  namespace: iofq
  annotations:
    kubernetes.io/ingress.class: "traefik"
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.middlewares: iofq-redirect-http@kubernetescrd
spec:
  rules:
  - host: ip.10110110.xyz
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ip-service
            port:
              number: 8080
