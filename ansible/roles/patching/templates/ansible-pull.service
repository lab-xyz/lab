{{ ansible_managed|comment }}

[Unit]
Description=Automated ansible-pull
After=network-online.target
 
[Service]
ExecStart=/bin/sh -c 'ansible-pull -U "{{ ansible_pull_url }}" {{ ansible_entrypoint }}'
TimeoutStopSec=600
